CREATE DATABASE labbdv

use labbdv

CREATE TABLE COLLABORATOR (
    ID_COLLABORATOR INT AUTO_INCREMENT NOT NULL,
    NAME VARCHAR (30),
    AGE INT (2),
    CPF VARCHAR (11),
    DEPARTMENT_ID int,
    PRIMARY KEY (ID_COLLABORATOR),
   
    );

ALTER TABLE Collaborator
ADD CONSTRAINT R_18 FOREIGN KEY (DEPARTMENT_ID) REFERENCES Department (ID_DEPARTMENT);


CREATE TABLE DEPARTMENT (
    ID_DEPARTMENT INT AUTO_INCREMENT NOT NULL,
    NAME VARCHAR (30),
    DESCRIPTION VARCHAR (30),
    OBSERVATION VARCHAR (30),
    PRIMARY KEY (ID_DEPARTMENT)
);